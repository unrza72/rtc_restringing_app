node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# local SQLite database (contains password hashes — never commit)
*.db
*.db-journal
*.db-wal
*.db-shm

# generated Prisma client (recreate with `pnpm db:generate`)
src/generated/
